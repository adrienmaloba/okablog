class Article {
  String title;
  String author;
  String description;
  String category;
  DateTime publication;
  String image;

  Article(this.title, this.author, this.description, this.category,
      this.publication, this.image);

}